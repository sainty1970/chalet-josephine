---
/**
 * Reviews Section
 * Guest testimonials
 */

const reviews = [
  {
    name: 'Bassam A.',
    rating: 5,
    text: 'We had a wonderful stay at Chalet Josephine. The place is beautifully designed with a high-tech, high-quality feel. Special thanks to the concierge team for being exceptionally helpful and flexible throughout our stay. A fantastic stay and we would happily return.',
    date: 'November 2025'
  },
  {
    name: 'Jose A.',
    rating: 5,
    text: 'We loved Chamonix and this house. The teens walked to town and we had dinners all of us together every night at the long dining table by the kitchen, with a few logs burning in the fireplace. Already discussing our 2026 Spring break at Josephine again!',
    date: 'March 2025'
  },
  {
    name: 'Rishi P.',
    rating: 5,
    text: 'The best thing about Chalet Josephine was how effortlessly it brought our entire extended family together. Spacious enough for everyone to have their own corner, yet cozy enough to make every meal and every laugh feel intimate. The views were breathtaking, the interiors thoughtfully designed. It became part of the story we\'ll be telling for years.',
    date: 'August 2025'
  }
];
---

<section class="section-spacing">
  <div class="container">
    <div class="section-header">
      <h2>What Our Guests Say</h2>
      <p class="section-subtitle">
        Discover why families return year after year
      </p>
    </div>

    <div class="reviews-grid">
      {reviews.map(review => (
        <div class="review-card">
          <div class="review-rating">
            {[...Array(review.rating)].map(() => (
              <span class="star">★</span>
            ))}
          </div>
          <p class="review-text">"{review.text}"</p>
          <div class="review-author">
            <p class="author-name">{review.name}</p>
            <p class="review-date">{review.date}</p>
          </div>
        </div>
      ))}
    </div>

    <div class="reviews-cta">
      <a href="/contact" class="btn btn-primary">Book Your Stay</a>
    </div>
  </div>
</section>

<style>
  .section-header {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .section-header h2 {
    font-size: var(--text-4xl);
    color: var(--color-charcoal);
    margin-bottom: var(--space-4);
  }

  .section-subtitle {
    font-size: var(--text-lg);
    color: var(--color-slate);
    max-width: 600px;
    margin: 0 auto;
  }

  .reviews-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--space-8);
    margin-bottom: var(--space-12);
  }

  .review-card {
    background: var(--color-pearl);
    padding: var(--space-8);
    border-radius: var(--radius-xl);
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  .review-rating {
    display: flex;
    gap: var(--space-1);
  }

  .star {
    color: var(--color-gold);
    font-size: var(--text-xl);
  }

  .review-text {
    font-size: var(--text-base);
    color: var(--color-charcoal);
    line-height: var(--leading-relaxed);
    font-style: italic;
    flex-grow: 1;
  }

  .review-author {
    padding-top: var(--space-4);
    border-top: 1px solid var(--color-stone);
  }

  .author-name {
    font-weight: var(--font-weight-semibold);
    color: var(--color-charcoal);
    margin-bottom: var(--space-1);
  }

  .review-date {
    font-size: var(--text-xs);
    color: var(--color-slate);
  }

  .reviews-cta {
    text-align: center;
    margin-top: var(--space-12);
  }

  @media (max-width: 640px) {
    .section-header h2 {
      font-size: var(--text-3xl);
    }

    .reviews-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
