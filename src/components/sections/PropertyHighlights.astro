---
/**
 * Property Highlights Section
 * Key features and amenities
 */

const highlights = [
  {
    icon: '🏔️',
    title: '6 Bedrooms',
    description: 'Sleeps up to 12 guests in luxurious comfort'
  },
  {
    icon: '⛷️',
    title: 'Ski-In/Ski-Out',
    description: 'Direct access to Chamonix\'s world-class slopes'
  },
  {
    icon: '🏊',
    title: 'Hot Tub & Spa',
    description: 'Relax with stunning Mont Blanc views'
  },
  {
    icon: '👨‍🍳',
    title: 'Gourmet Kitchen',
    description: 'Chef-ready with premium appliances'
  },
  {
    icon: '🔥',
    title: 'Fireplace & Lounge',
    description: 'Cozy alpine atmosphere throughout'
  },
  {
    icon: '🅿️',
    title: 'Private Parking',
    description: 'Secure covered parking for 2 vehicles'
  }
];
---

<section class="section-spacing bg-pearl">
  <div class="container">
    <div class="section-header">
      <h2>Alpine Luxury Redefined</h2>
      <p class="section-subtitle">
        Discover the perfect blend of traditional Savoyard charm and modern luxury
      </p>
    </div>

    <div class="highlights-grid">
      {highlights.map(highlight => (
        <div class="highlight-card">
          <div class="highlight-icon">{highlight.icon}</div>
          <h3 class="highlight-title">{highlight.title}</h3>
          <p class="highlight-description">{highlight.description}</p>
        </div>
      ))}
    </div>

    <div class="highlights-cta">
      <a href="/about" class="btn btn-primary">Explore the Chalet</a>
    </div>
  </div>
</section>

<style>
  .section-header {
    text-align: center;
    margin-bottom: var(--space-16);
  }

  .section-header h2 {
    font-size: var(--text-4xl);
    color: var(--color-charcoal);
    margin-bottom: var(--space-4);
  }

  .section-subtitle {
    font-size: var(--text-lg);
    color: var(--color-slate);
    max-width: 600px;
    margin: 0 auto;
  }

  .highlights-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-8);
    margin-bottom: var(--space-12);
  }

  .highlight-card {
    background: white;
    padding: var(--space-8);
    border-radius: var(--radius-xl);
    text-align: center;
    transition: transform var(--transition-base), box-shadow var(--transition-base);
    box-shadow: var(--shadow-sm);
  }

  .highlight-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .highlight-icon {
    font-size: 3rem;
    margin-bottom: var(--space-4);
  }

  .highlight-title {
    font-family: var(--font-heading);
    font-size: var(--text-xl);
    color: var(--color-charcoal);
    margin-bottom: var(--space-2);
  }

  .highlight-description {
    font-size: var(--text-sm);
    color: var(--color-slate);
    line-height: var(--leading-relaxed);
  }

  .highlights-cta {
    text-align: center;
    margin-top: var(--space-12);
  }

  @media (max-width: 640px) {
    .section-header h2 {
      font-size: var(--text-3xl);
    }

    .highlights-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
